calendar.geodesync.com {
	# Port 80 isn't forwarded (ISP/router reserves it), so certificate
	# issuance/renewal relies solely on the TLS-ALPN-01 challenge over 443.
	tls {
		issuer acme {
			disable_http_challenge
		}
	}
	reverse_proxy app:4000
}

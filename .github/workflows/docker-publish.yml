name: Build and publish Docker image

on:
  push:
    branches: [main]
  workflow_dispatch: {}

permissions:
  contents: read
  packages: write

# Only the newest commit on a branch is worth building: the image it publishes
# is `latest`, and a slower older build finishing afterwards would re-tag
# `latest` backwards to the commit it started from. Cancelling also keeps
# several builds from evicting each other's layers out of the shared cache.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-push:
    # An arm64 runner, because the image is arm64: the Mac mini it's deployed
    # to is Apple Silicon. Building it on the x86 runner meant emulating every
    # command through QEMU — a C++ compile of better-sqlite3 and a full Next
    # build, twice over — which is what turned a cold build into an hour.
    runs-on: ubuntu-24.04-arm
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Record build timestamp
        id: build-meta
        run: echo "time=$(date -u +%Y-%m-%dT%H:%M:%SZ)" >> "$GITHUB_OUTPUT"

      - uses: docker/build-push-action@v6
        with:
          context: .
          build-args: |
            GIT_COMMIT=${{ github.sha }}
            BUILD_TIME=${{ steps.build-meta.outputs.time }}
          # Nothing pulls the amd64 image. Add it back with a second job on
          # ubuntu-latest and a manifest merge rather than by emulating it here.
          platforms: linux/arm64
          push: true
          tags: |
            ghcr.io/${{ github.repository }}:latest
            ghcr.io/${{ github.repository }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

